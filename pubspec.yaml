# pubspec.yaml
# Add this dependency to your existing pubspec.yaml file

name: academic_planner
description: Academic planner app for ALU students

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # For date formatting (required for dashboard)
  intl: ^0.18.0
  
  # Uncomment when implementing data persistence
  # shared_preferences: ^2.2.2
  # sqflite: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true